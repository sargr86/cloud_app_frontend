import { GetMatTableDataSourcePipe } from './get-mat-table-data-source.pipe';

describe('GetMatTableDataSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new GetMatTableDataSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
