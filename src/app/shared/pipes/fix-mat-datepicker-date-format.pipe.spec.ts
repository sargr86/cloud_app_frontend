import { FixMatDatepickerDateFormatPipe } from './fix-mat-datepicker-date-format.pipe';

describe('FixMatDatepickerDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FixMatDatepickerDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
