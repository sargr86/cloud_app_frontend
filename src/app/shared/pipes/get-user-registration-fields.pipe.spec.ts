import { GetUserRegistrationFieldsPipe } from './get-user-registration-fields.pipe';

describe('GetUserRegistrationFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetUserRegistrationFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
